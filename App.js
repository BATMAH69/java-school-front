import React, {Component} from 'react';
import { StyleSheet, Text, View } from 'react-native';
import NativeExample from './components/App';

export default class App extends Component {
  render() {
    return (
      <NativeExample />
    );
  }
}